// Placeholder test file.
// This prevents `flutter create .` in CI from generating the default MyApp test,
// which would fail analysis because this project uses a different app entrypoint.
void main() {}
